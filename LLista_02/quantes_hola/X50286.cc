#include "string"
#include "iostream"

using namespace std;

int main () {
    char c;
    int count = 0;
    
    while (cin >> c) {
        
        if (c=='h'){
            cin>>c;
            if (c=='o'){
                cin>>c;
                if (c=='l'){
                    cin>>c;
                    if (c=='a'){
			count = count + 1;
			                    }}}}
        
        
    }
    cout << count << endl;
}